#include "main.h"
/**
 * mul -  a function that multiplies two integers
 * @a: a - First int
 * @b: b - Second int
 * Return: The mul of a and b.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
